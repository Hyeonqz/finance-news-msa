spring:
  application:
    name: intelligence-service

  profiles:
    active: local

  config:
    import: "configserver:http://localhost:8888"

  # cloud config
  cloud:
    config:
      fail-fast: true